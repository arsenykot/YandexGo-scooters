# React + FastAPI (archived)

This folder contains the previous full-stack implementation. It is no longer the main way to run the project.

- `backend/` — FastAPI API
- `frontend/` — React + Vite app
- `requirements.txt` — Python dependencies for the backend
- `static.zip` — earlier static build archive
